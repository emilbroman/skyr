query Repositories {
  repositories {
    name
    environments {
      name
      qid
      deployments {
        id
        ref
        state
        commit {
          hash
          message
        }
      }
    }
  }
}
