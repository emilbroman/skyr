query AuthChallenge($username: String!) {
  authChallenge(username: $username) {
    challenge
  }
}

mutation SignIn($username: String!, $proof: JSON!) {
  signin(username: $username, proof: $proof) {
    user {
      username
      email
      fullname
      publicKeys
    }
    token
  }
}

query RefreshToken {
  refreshToken {
    user {
      username
      email
      fullname
      publicKeys
    }
    token
  }
}

query Me {
  me {
    username
    email
    fullname
    publicKeys
  }
}
