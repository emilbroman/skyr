syntax = "proto3";

package rtp.v1;

import "google/protobuf/empty.proto";

service ResourceTransitionPlugin {
  rpc ExchangeCapabilities(CapabilityRequest) returns (CapabilityResponse);
  rpc CreateResource(CreateResourceRequest) returns (CreateResourceResponse);
  rpc UpdateResource(UpdateResourceRequest) returns (UpdateResourceResponse);
  rpc DeleteResource(DeleteResourceRequest) returns (google.protobuf.Empty);
  rpc Check(CheckRequest) returns (CheckResponse);
}

message CapabilityRequest {
  string plugin_name = 1;
  string plugin_version = 2;
}

message CapabilityResponse {
  string protocol_version = 1;
  repeated string capabilities = 2;
}

message CreateResourceRequest {
  string resource_type = 1;
  string resource_name = 2;
  string resource_inputs_json = 3;
  // Fully-qualified deployment ID (format:
  // "OrgId/RepoId::EnvironmentId@DeploymentId.Nonce").
  string deployment_qid = 4;
}

enum Marker {
  VOLATILE = 0;
  STICKY = 1;
}

message Resource {
  string type = 1;
  string name = 2;
  string inputs_json = 3;
  string outputs_json = 4;
  repeated Marker markers = 5;
}

message CreateResourceResponse {
  Resource resource = 1;
}

message UpdateResourceRequest {
  Resource resource = 1;
  string inputs_json = 2;
  string deployment_qid = 3;
}

message UpdateResourceResponse {
  Resource resource = 1;
}

message DeleteResourceRequest {
  Resource resource = 1;
  string deployment_qid = 2;
}

message CheckRequest {
  Resource resource = 1;
  string deployment_qid = 2;
}

message CheckResponse {
  Resource resource = 1;
}
