mutation Signup($username: String!, $email: String!, $proof: JSON!) {
  signup(username: $username, email: $email, proof: $proof) {
    user {
      username
      email
      fullname
    }
    token
  }
}
