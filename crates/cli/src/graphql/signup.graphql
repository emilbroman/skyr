mutation Signup($username: String!, $email: String!, $proof: JSON!, $region: String!, $fullname: String) {
  signup(username: $username, email: $email, proof: $proof, region: $region, fullname: $fullname) {
    user {
      username
      email
      fullname
    }
    token
  }
}
