mutation Signup($username: String!, $email: String!, $proof: JSON!, $fullname: String) {
  signup(username: $username, email: $email, proof: $proof, fullname: $fullname) {
    user {
      username
      email
      fullname
    }
    token
  }
}
