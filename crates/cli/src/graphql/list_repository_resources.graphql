query ListRepositoryResources($organization: String!, $repository: String!) {
  organization(name: $organization) {
    repository(name: $repository) {
      name
      environments {
        name
        qid
        resources {
          type
          name
          inputs
          outputs
          owner {
            id
          }
        }
      }
    }
  }
}
