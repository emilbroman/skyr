subscription EnvironmentLogs($environmentQid: String!, $initialAmount: Int) {
  environmentLogs(environmentQid: $environmentQid, initialAmount: $initialAmount) {
    severity
    timestamp
    message
  }
}
