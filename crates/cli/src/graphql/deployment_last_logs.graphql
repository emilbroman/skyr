query DeploymentLastLogs($amount: Int) {
  repositories {
    name
    environments {
      name
      qid
      deployments {
        qid
        state
        lastLogs(amount: $amount) {
          severity
          timestamp
          message
        }
      }
    }
  }
}
