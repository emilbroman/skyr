query ResourceLastLogs($amount: Int) {
  repositories {
    name
    environments {
      name
      qid
      resources {
        type
        name
        lastLogs(amount: $amount) {
          severity
          timestamp
          message
        }
      }
    }
  }
}
