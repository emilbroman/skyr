[package]
name = "ids"
version = "0.1.0"
edition = "2024"

[features]
rand = ["dep:rand"]

[dependencies]
rand = { version = "0.9.2", optional = true }
serde = { version = "1.0.228", features = ["derive"] }
thiserror = "2.0.18"

[lints]
workspace = true
